<div id="book-page">
  <a href="/books" class="action-btn">Voltar</a>
  <div class="container">
    <h1>{{book.title}}</h1>
    <p>Este livro tem {{book.pageqty}} páginas.</p>
    <div class="actions">
      <a href="/books/edit/{{book.id}}" class="action-btn edit-btn">Editar</a>
      <form action="/books/remove/{{book.id}}" method="POST">
        <input type="submit" value="Remover" class="action-btn delete-btn">
      </form>
    </div>
  </div>
</div>